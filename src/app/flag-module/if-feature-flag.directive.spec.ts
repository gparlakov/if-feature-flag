import { IfFeatureFlagDirective } from './if-feature-flag.directive';

describe('IfFeatureFlagDirective', () => {
  it('should create an instance', () => {
    const directive = new IfFeatureFlagDirective();
    expect(directive).toBeTruthy();
  });
});
